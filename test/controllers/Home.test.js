// require('test/helpers/specHelper')
// const { request, asset } = require('chai')
// const app = require('test/helpers/app')
// const testUrl = require('test/helpers/testUrl')

// describe('initialize and setup express application', () => {
//   describe('app initialized', () => {
//     it('app is initialized', async () => {
//       const response = request.get(testUrl('test'))
//       response.deepEqual(response.body,  {test: 'test'})
//     })
//   })
// })
